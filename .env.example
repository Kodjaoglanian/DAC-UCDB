# Arquivo de exemplo de configuração
# Copie este arquivo para .env e configure conforme seu ambiente

# === CONFIGURAÇÃO DO MONGODB ===
# Para desenvolvimento local:
# MONGODB_URI=mongodb://127.0.0.1:27017/dacdb

# Para MongoDB Atlas (nuvem), use uma URI como:
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/dacdb?retryWrites=true&w=majority

# Para MongoDB em container Docker ou outro servidor:
# MONGODB_URI=mongodb://host:porta/dacdb

# === CONFIGURAÇÃO DO BANCO ===
MONGODB_DB=dacdb
MONGODB_COLLECTION=usuarios

# === CONFIGURAÇÃO DO SERVIDOR ===
# Porta do backend Python
PY_BACKEND_PORT=5000

# Porta do servidor Node.js (pode ser definida pela plataforma de hospedagem)
# PORT=3000